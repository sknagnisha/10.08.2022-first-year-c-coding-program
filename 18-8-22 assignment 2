#include <stdio.h>

int main() {
int arr[3],sum=0,fact=1,i;
for(i=0;i<3;i++)
scanf("%d",&arr[i]);
for(i=0;i<3;i++)
{
    fact=1;
    while(arr[i]!=0)
    {
        
        fact=fact*arr[i];
        arr[i]--;
    }
    sum=sum+fact;
}
printf("%d",sum);
    return 0;
}

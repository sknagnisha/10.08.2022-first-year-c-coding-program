#include <stdio.h>

int main() {
int arr[100][100],i,j,r,c,sum=0;
scanf("%d%d",&r,&c);
for(i=0;i<r;i++)
{
    for(j=0;j<c;j++)
    scanf("%d",&arr[i][j]);
    printf("\n");
}
for(j=0;j<c;j++)
{
    sum=0;
    for(i=0;i<r;i++)
    {
    sum=sum+arr[i][j];
    }
    printf("%d column sum=%d\n",j+1,sum);
    
}
   

    return 0;
}
